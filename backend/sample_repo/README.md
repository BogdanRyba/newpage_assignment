# sample_repo

Fixture codebase ingested by the seeder so Ariadne has something to answer about immediately
after `docker compose up`. Small on purpose; contains a Python calculator and a TypeScript
tokenizer with real, citable symbols.
